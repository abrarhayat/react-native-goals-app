import { StyleSheet } from 'react-native'
const styles = StyleSheet.create({
    screen: {
        padding: 60,
        flex: 1,
        backgroundColor: 'rgba(0, 10, 2, 0.7)'
    }
});

export default styles.screen;